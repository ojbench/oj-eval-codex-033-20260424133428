#include <bits/stdc++.h>
#include "src.hpp"

int main() {
  // No I/O required for library assignment. Keep process trivial.
  // Prevent optimization removing everything.
  sjtu::list<int> lst; lst.push_back(1); lst.pop_back();
  return 0;
}

